{
  "name": "http-stack",
  "description": "A `StreamStack` implementation of the HTTP protocol.",
  "version": "0.2.0",
  "author": "Nathan Rajlich <nathan@tootallnate.net>",
  "keywords": ["stream", "stack", "http", "protocol"],
  "dependencies" : { "stream-stack" : ">= 1.1.0" },
  "devDependencies" : {
    "colors": ">= 0.3.0",
    "expresso": "*"
  },
  "main": "./lib/http-stack",
  "modules": {
    "client": "./lib/client",
    "server": "./lib/server"
  },
  "scripts": { "test": "expresso test/test*.js" },
  "engines": { "node": ">= 0.3.0" }
}
